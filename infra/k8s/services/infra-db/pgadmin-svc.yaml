apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: infra-db
spec:
  type: ClusterIP
  selector:
    app: pgadmin
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
