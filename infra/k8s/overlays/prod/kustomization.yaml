apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: Create Secret bind9-tsig in dns-zone namespace before apply
# See: infra/k8s/components/dns-zone/tsig-secret.example.yaml

resources:
  - ../../base
  - ../../components/cert-manager
  - ../../components/kyverno
  - ../../components/dns-zone
  - ../../components/infra-db
  - ../../components/mail-zone
  - ../../components/platform

# Patches can be added here for environment-specific overrides
# patches:
#   - path: patches/example-patch.yaml
#     target:
#       kind: Deployment
#       name: example
