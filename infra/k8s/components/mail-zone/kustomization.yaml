apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - mailcow-auth.yaml
  - mailcow-core.yaml
  # mailcow-ingress.yaml excluded - contains REPLACE_MAILCOW_DOMAIN placeholder
  - networkpolicy.yaml
  - traefik-tcp/ingressroutetcp.yaml
