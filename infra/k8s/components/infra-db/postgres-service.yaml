apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: infra-db
  labels:
    app: postgres
    app.kubernetes.io/part-of: voxeil
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
