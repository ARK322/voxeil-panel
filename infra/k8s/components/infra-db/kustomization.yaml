apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - networkpolicy.yaml
  # pgadmin-auth.yaml excluded - requires Traefik Middleware CRDs
  - pgadmin-deploy.yaml
  # pgadmin-ingress.yaml excluded - contains REPLACE_PGADMIN_DOMAIN placeholder
  - pgadmin-secret.yaml
  - pgadmin-svc.yaml
  - postgres-secret.yaml
  - postgres-service.yaml
  - postgres-statefulset.yaml
