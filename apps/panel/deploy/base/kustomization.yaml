apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform

resources:
  - deployment.yaml
  - service.yaml
  # ingress.yaml excluded - contains REPLACE_PANEL_DOMAIN placeholder
  # auth.yaml excluded - requires Traefik Middleware CRDs and contains placeholder Secret
  # redirect.yaml excluded - requires Traefik Middleware CRDs
