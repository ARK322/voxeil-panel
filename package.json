{
  "name": "voxeil-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Voxeil Panel - K3s/Kubernetes hosting panel infrastructure",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:ci": "npx tsc --build && cd apps/panel && npm run build && cd ../..",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
