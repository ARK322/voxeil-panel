{
  "name": "voxeil-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Voxeil Panel - K3s/Kubernetes hosting panel infrastructure",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:packages": "npx --package=typescript@5.6.3 tsc --build",
    "build:ci": "npm run build:packages && npm install && npm run build -w vhp-panel",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
