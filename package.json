{
  "name": "voxeil-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Voxeil Panel - K3s/Kubernetes hosting panel infrastructure",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:ci": "npm run build -w @voxeil/shared && node -e \"require('fs').accessSync('packages/shared/dist/types.d.ts')\" && npm run build -w @voxeil/api-client && npm run build -w vhp-panel",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
